# UniversalAPI Local Development Environment
# Copy to .env.local and customize
#
# This file is used by honcho (Procfile.local) and Makefile

# Logs directory (relative to project root)
LOGS_DIR=./logs

# Which processes to start (comma-separated, or 'all')
# Options: docker,backend,frontend,celery_worker,celery_beat
# PROCESSES=all

# Enable colored output in honcho
HONCHO_COLORS=true
